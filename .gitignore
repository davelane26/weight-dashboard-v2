# Secrets — never commit these
.github_token
.env
*.env
garmin_sync/.env
garmin_sync/cookies.json
garmin_sync/.garth/
glapp_sync/glapp_state.json
glapp_sync/.glapp_venv/
glapp_sync/sync.log

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
server.log  
shots/ 
shots-v2/ 
server.log 
_*.py

# Local dev-only data snapshot (generated from openScale CSV for localhost
# preview). Production fetches from the token-gated worker / remote data.json.
data.json
